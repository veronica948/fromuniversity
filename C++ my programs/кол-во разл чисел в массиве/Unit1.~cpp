#include<iostream.h>
int main()
{const int N=10;
int a[N]={1,3,5,3,5,5,6,3,0,4};
int p=N;
for(int i=0;i<N;i++)
     {for(int j=i+1;j<N;j++)
     if(a[i]==a[j]) f=1;
          //{for(int k=j;k<p-1;k++)
                //a[k]=a[k+1];
                if(f) 
          p--;
      }    //j--;

cout<<"The number of different numbers="<<p<<endl;
system("pause");
return 0;
}
 